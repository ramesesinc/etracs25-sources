<% 
    def flavor = "default" 
    def themeName = THEME.name ? THEME.name : "default";
    def ver = (OPTIONS.ver ? OPTIONS.ver : 1.0);
%>
<link href="/themes/${themeName}/css/${flavor}/${OPTIONS.value}.css?ver=${ver}" type="text/css" rel="stylesheet" />
